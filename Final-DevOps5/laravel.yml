apiVersion: apps/v1
kind: Deployment
metadata:
  name: laravel
spec:
  selector:
    matchLabels:
      app: laravel
  replicas: 1
  template:
    metadata:
      labels:
        app: laravel
    spec:
      containers:
      - name: laravel
        image: FROM sovichea10/laravel-app-7.3:v4/laravel-app
        ports:
        - containerPort: 9000
        env:
        - name: DB_HOST
          value: mysql
        - name: DB_DATABASE
          value: hieklymonyratanak-data.db
        - name: DB_USERNAME
          value: root
        - name: DB_PASSWORD
          value: Hello@123
---
apiVersion: v1
kind: Service
metadata:
  name: laravel
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 9000
  selector:
    app: laravel
